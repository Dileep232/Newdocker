pipeline {
  agent any
  stages {
    stage ('code checkout') {
      steps {
      }
    }
    stage ('creating image with docker file') {
      steps {
      }
    }
    stage ('deploying image to container') {
      steps {
      }
    }
  }
}
